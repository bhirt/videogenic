import React from 'react';

import WorkflowWrapper from './WorkflowWrapper';
import Split from './Split';

export default class SplitWorkflow extends React.Component {
    render() {
        return <WorkflowWrapper component={Split} />
    }
}

